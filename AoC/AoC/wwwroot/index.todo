<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title></title>
</head>
<body>
    <script>

        var req = new XMLHttpRequest();
        req.addEventListener('readystatechange', function () {
            var xhr = this;
            if (xhr.readyState === 4 && xhr.status === 200) {
                var resp = xhr.responseText;

                var regex = /a href="(\.\/\d+\/)"/gm;

                var match = regex.exec(resp);

                
                
                debugger;
            }
        });
        req.open('GET', '/' + new Date().getFullYear() + '/');
        req.send();
    </script>
</body>
</html>